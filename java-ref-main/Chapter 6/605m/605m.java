/*	Chapter 6.6 Passing Objects as Arguments

	--> when passing an object as an argument to a parameter variable, the object's address is actually the one being passed into the method's parameter variable
	--> thus, the parameter references the object
*/

public class Medium605 {

	public static void main(String[] nu) {
		
	}
}
